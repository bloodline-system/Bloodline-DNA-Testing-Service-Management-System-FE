/**
 * selectors.js — Post Management selector helper.
 *
 * Kept as a thin wrapper around the existing selector constants
 * to comply with the requested file structure.
 */

export { POST_TEXT, PW } from "./post-management.selectors.js";
